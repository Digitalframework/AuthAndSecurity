# Copy to .env (git-ignored) and fill in the values from Google Cloud Console,
# or export these as real environment variables instead.
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret